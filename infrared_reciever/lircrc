begin
   prog   = irexec
   button = KEY_POWER
   config = /sbin/reboot
end

begin
   prog   = irexec
   button = KEY_1
   config = /bin/sh -c "ls > /tmp/vasa1"
end

begin
   prog   = irexec
   button = KEY_2
   config = /bin/sh -c "ls > /tmp/vasa2"
end

begin
   prog   = irexec
   button = KEY_3
   config = /bin/sh -c "ls > /tmp/vasa3"
end

begin
   prog   = irexec
   button = KEY_4
   config = /bin/sh -c "ls > /tmp/vasa4"
end

begin
   prog   = irexec
   button = KEY_5
   config = /bin/sh -c "ls > /tmp/vasa5"
end

begin
   prog   = irexec
   button = KEY_6
   config = /bin/sh -c "ls > /tmp/vasa6"
end

